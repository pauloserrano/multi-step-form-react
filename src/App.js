import UserForm from "./components/UserForm";

function App() {
  return (
    <>
      <UserForm />
    </>
  );
}

export default App;

const styles = {
  container: {
      display: 'flex',
      alignItems: 'center',
      flexDirection: 'column',
  }
}